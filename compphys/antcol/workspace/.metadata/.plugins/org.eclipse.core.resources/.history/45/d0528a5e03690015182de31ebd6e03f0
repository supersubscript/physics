import java.awt.Color;
import java.awt.Graphics;
import java.util.ArrayList;

import javax.swing.JPanel;

public class AntPanel extends JPanel
{

   public AntPanel(final int width, final int height)
   {
   }

   public void paintComponent(Graphics g)
   {
      g.setColor(Color.black);
   }
   
   // Takes in squares (thank God for pointers), 
   public void paint(Square[][] squares, int sqSize, ArrayList<Food> foodPos, ArrayList<Anthill> anthills){
      
      // Draw all ants
      for (int i = 0; i < squares.length; i++)
         for (int j = 0; j < squares.length; j++)
         {
            
         }
      // Draw all food
      
      // Draw all anthills
      
      // Update(?) 
      
   }
   
}
